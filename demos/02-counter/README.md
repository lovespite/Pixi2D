点击 +1 / 重置 计数；右侧开关切换是否打印每次变化到控制台。

# 02-counter

入门级脚本 demo：演示 `on-click` 与 `obj.on('changed', fn)` 两种事件订阅形式。

控件：`button × 2`、`switch`、`fancy-text`。

启动：

```powershell
.\demos\run.ps1 -Name counter
```
