进度条动画：演示 setInterval 平滑驱动 progress-bar.value。

# 12-progress-animate

★ 依赖 Host Pump。

控件：`progress-bar`、`button × 2`、`fancy-text`。
点 "开始" 后 16ms 间隔小步增长，到 100% 自动停止；可暂停。

启动：

```powershell
.\demos\run.ps1 -Name animate
```
